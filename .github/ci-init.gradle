allprojects {
    repositories {
        // Add critical repositories first for CI
        maven { url 'https://jcenter.bintray.com' }
        maven { url 'https://jitpack.io' }
        mavenCentral()
        google()
    }
}